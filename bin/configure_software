#! /usr/bin/env bash

# Configures installed software.

# Homebrew
(
  cd "$(get_homebrew_root)"
  git config --local core.hooksPath /dev/null
)

apm install sync-settings
# will need to create a gist token and enter it together with gist id into sync-settings preferences 
