#! /usr/bin/env bash

# Installs Homebrew software.

# Readline
# brew install readline

# OpenSSL 
brew install openssl

# OpenSSL OSX CA rm 2025
#brew tap raggi/ale
#brew install openssl-osx-ca

# asciinema rm 2025 -- dependency ca-certificates got stuck 
# brew install asciinema

# Ant
brew install ant

# Bash
brew install bash

# Bash Completion
brew install bash-completion

# Colorized Cat
brew install ccat

# Composer dependancy manage for php
# Install later when you decide on PHP deafult reqs
# brew install composer

# CTags
brew install ctags

# Diff So Fancy
brew install diff-so-fancy

#dnsmasq
brew install dnsmasq

# Fish
brew install fish

##brew install glib rm 2025

# Gawk
brew install gawk

# GDBM rm 2025
# brew install gdbm

# Git
brew install git
brew install git-lfs
git lfs install

# GPG
#brew install gpg

# GPG Agent
#brew install gpg-agent

# grep will be installed as ggrep
brew install grep

# Htop
brew install htop

# Apache
# brew tap homebrew/apache
# brew install httpd --with-privileged-ports --with-http2

# HTTPie
brew install httpie

# ImageMagick
brew install imagemagick

# Unicode Components
brew install icu4c

# Java Environment
brew install jenv

# JSON CLI Parser
brew install jq

# lib xml
brew install libxml2

# lnav
brew install lnav

# Lynis
brew install lynis

# Mail Tracker Blocker rm2025 see
# https://github.com/apparition47/MailTrackerBlockerApp?tab=readme-ov-file
# but there is currently a bug
#brew install mailtrackerblocker 

# Maven
brew install maven

# Mac App Store CLI
brew install mas

# Mysql - decide later
brew install mysql

# Nginx
brew install nginx

# Node
brew install node

# Pandoc
# brew install pandoc

# Parallel
# brew install parallel

# Peco
brew install peco

# PHP
# does tapping happen here?
# brew tap homebrew/dupes
# brew tap homebrew/versions
# brew tap homebrew/php
# brew install php56 --with-httpd24
# brew install homebrew/php/php56-mcrypt
# brew install homebrew/php/php56-opcache
# brew install homebrew/php/phpunit
brew tap shivammathur/php
brew install shivammathur/php/php@5.6
brew install shivammathur/php/php@7.4
brew install shivammathur/php/php@8.0
brew install shivammathur/php/php@8.1
brew install shivammathur/php/php@8.2
brew install shivammathur/php/php@8.3
brew install shivammathur/php/php@8.4


#p7zip
brew install p7zip

# Python rm 2025
# brew install python@3.9

# Redis
# brew install redis

# Ruby Install
brew install ruby-install ruby rbenv

# Saxon
brew install saxon

# ShellCheck
#brew install shellcheck

#Silver Searcher
brew install the_silver_searcher

# Subversion
# brew install subversion

# Tag
brew install tag

# Terminal Notifier
brew install terminal-notifier

# Tree
brew install tree

# Volta
brew install volta

# Watchman
brew install watchman

# Wget
brew install wget

# xz
brew install xz

#Yarn
brew install yarn

# Z
# z foo         cd to most frecent dir matching foo
# z foo bar     cd to most frecent dir matching foo, then bar
# z -r foo      cd to highest ranked dir matching foo
# z -t foo      cd to most recently accessed dir matching foo
# z -l foo      list all dirs matching foo (by frecency)
brew install z

# zlib
brew install zlib
